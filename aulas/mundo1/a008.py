from math import sqrt, floor    # importando especificamente funções da biblioteca math do python
import random                   # importando a funcao random,

# calculando a raiz quadrada de um número usandoa a biblioteca math com a funcao sqrt(x)
# numero = int(input("Digite um número: "))
# raiz_quadrada = sqrt(numero)
# print("Raiz Quadrada do número {} é: {}.".format(numero, floor(raiz_quadrada)))

# usando a random
numero_2 = random.randint(1, 100)
print(numero_2)
